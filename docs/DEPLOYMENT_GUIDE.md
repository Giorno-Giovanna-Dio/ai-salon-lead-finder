# 🚀 雲端部署完整指南

## 架構概覽

```
┌─────────────────────────────────────────────────────┐
│                    使用者訪問                        │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  Vercel (免費 Hobby Plan)                           │
│  • Next.js Dashboard                                │
│  • API Routes                                       │
│  成本: $0/月                                        │
└─────────────────────────────────────────────────────┘
         ↓                                    ↓
┌──────────────────────┐         ┌──────────────────────┐
│ Supabase Pro         │         │ Railway / Render     │
│ • PostgreSQL 資料庫  │         │ • Crawler Worker     │
│ • Storage (圖片)     │         │ • OpenClaw Browser   │
│ 成本: $25/月         │         │ 成本: $5-10/月       │
└──────────────────────┘         └──────────────────────┘
```

## 需要的服務

### 1. ✅ Supabase Pro Plan（您已有）

**用途：**
- PostgreSQL 資料庫（存放所有資料）
- Storage（存放 DM 圖片）

**費用：** $25/月

**包含：**
- 8GB 資料庫
- 100GB Storage
- 無限 API 請求
- 無 pausing（不會自動暫停）

**設定需求：**
- ✅ 需要更新：Database URL
- ✅ 需要更新：Project URL
- ✅ 需要更新：API Keys (anon + service_role)

---

### 2. ✅ Vercel（推薦 - 免費就夠用！）

**用途：**
- 部署 Next.js Dashboard
- 處理 API Routes
- 提供 HTTPS 和全球 CDN

**費用：** $0/月（Hobby Plan）

**限制：**
- 每月 100GB 頻寬（對您的使用量絰綽有餘）
- Serverless Function 執行時間 10 秒
  - ⚠️ Crawler 不能在這裡跑（需要太長時間）
  - ✅ 其他 API 完全沒問題

**不需要額外付費的情況：**
- 單一團隊/個人使用
- 流量不超過 100GB/月

**何時需要升級到 Pro ($20/月)：**
- 需要多個團隊成員協作
- 需要更長的 Function 執行時間
- 流量超過 100GB

---

### 3. ✅ Railway 或 Render（Crawler Worker）

**用途：**
- 運行 Crawler（OpenClaw browser automation）
- 運行 DM Sender
- 運行 Response Tracker

**為什麼需要？**
- OpenClaw 需要運行 browser（Chrome/Chromium）
- 需要長時間執行（可能超過 10 秒）
- 需要保持 4 個 browser profiles 的 sessions

**選項比較：**

#### Railway（推薦）
- 費用：$5/月起
- 優點：設定簡單，支援 Docker
- 缺點：比 Render 稍貴一點

#### Render
- 費用：$7/月起
- 優點：穩定，有免費方案可測試
- 缺點：免費方案會 sleep（付費才能 24/7 運行）

---

## 總成本估算

### 最小配置（開發/小規模）

| 服務 | 用途 | 成本 |
|------|------|------|
| Supabase Pro | 資料庫 + Storage | $25/月 |
| Vercel Hobby | Dashboard 部署 | $0/月 |
| Railway Starter | Crawler Worker | $5/月 |
| **總計** | | **$30/月** |

### 成長配置（商業化後）

| 服務 | 用途 | 成本 |
|------|------|------|
| Supabase Pro | 資料庫 + Storage | $25/月 |
| Vercel Pro | Dashboard + 團隊協作 | $20/月 |
| Railway Pro | 更強的 Crawler Worker | $20/月 |
| **總計** | | **$65/月** |

---

## 完整部署流程

### 階段 1: 更新 Supabase Pro Plan 資訊

請告訴我新的 Supabase 專案資訊，我會幫您更新：

1. **新的 Project Reference**（網址中的那串）
   - 舊的：`rvgykowamfrhhparlbkl`
   - 新的：`???????`（請提供）

2. **新的 Database 密碼**（如果有改）

3. **API Keys**（一定要重新獲取）
   - anon key
   - service_role key

### 階段 2: 部署 Dashboard 到 Vercel

```bash
# 1. 安裝 Vercel CLI
npm install -g vercel

# 2. 登入 Vercel
vercel login

# 3. 部署
vercel

# 4. 設定環境變數（在 Vercel Dashboard）
# 前往 Project Settings > Environment Variables
# 新增所有 .env 中的變數
```

### 階段 3: 部署 Crawler Worker 到 Railway

```bash
# 1. 建立 crawler-worker 專案（我會幫您建立）
cd crawler-worker

# 2. 建立 Railway 專案
railway login
railway init

# 3. 部署
railway up
```

---

## 與 AWS + MySQL 的比較

### 您詢問的 AWS + MySQL 方案：

| 項目 | AWS + MySQL | Supabase Pro + Vercel |
|------|-------------|----------------------|
| **資料庫** | RDS MySQL ($15-30/月) | Supabase PostgreSQL (含) |
| **儲存** | S3 ($3-5/月) | Supabase Storage (含) |
| **應用部署** | EC2 ($10-20/月) | Vercel ($0) |
| **Crawler** | 同 EC2 或另開 | Railway ($5-10) |
| **CDN** | CloudFront ($5-10) | Vercel (含) |
| **HTTPS** | ACM (免費) + 設定 | Vercel 自動 |
| **維護難度** | ⭐⭐⭐⭐ 需自己管理 | ⭐ 全自動 |
| **總成本** | **$35-70/月** | **$30-45/月** |

**結論：Supabase + Vercel 更簡單、更便宜、更易維護**

---

## 立即行動清單

### 步驟 1: 提供新的 Supabase 資訊

請前往您的新 Supabase Pro 專案：

1. **專案 URL** 
   - 您在 Supabase Dashboard 的網址：`https://supabase.com/dashboard/project/[THIS_PART]`
   - 這個 `[THIS_PART]` 就是 Project Reference

2. **前往 API 設定**
   - 網址：`https://supabase.com/dashboard/project/[YOUR_PROJECT_REF]/settings/api`
   - 複製 **anon** key
   - 複製 **service_role** key

3. **前往 Database 設定**
   - 網址：`https://supabase.com/dashboard/project/[YOUR_PROJECT_REF]/settings/database`
   - 複製 Connection string (URI)

### 步驟 2: 告訴我資訊

把以下資訊告訴我，我會幫您更新所有配置：

```
1. Project Reference: ________
2. Database Password: ________ （如果還是 aisalonleadautomation 就不用改）
3. Anon Key: eyJ...
4. Service Role Key: eyJ...
```

### 步驟 3: 我會幫您

- ✅ 更新所有配置檔案
- ✅ 測試資料庫連線
- ✅ 準備 Vercel 部署配置
- ✅ 準備 Railway 部署配置

---

## 常見問題

### Q: 一定要用 Railway 嗎？可以用其他的嗎？

A: 可以！以下都可以：
- ✅ **Railway** - 最簡單（推薦）
- ✅ **Render** - 也不錯
- ✅ **Fly.io** - 很便宜
- ✅ **DigitalOcean App Platform** - 穩定
- ❌ **Vercel** - 不適合（Function timeout 太短）
- ❌ **AWS Lambda** - 不適合（cold start + timeout）

### Q: 能不能全部自己架在 AWS EC2？

A: 可以，但不推薦：
- 需要自己管理 SSL 憑證
- 需要自己設定 Load Balancer
- 需要自己處理資料庫備份
- 成本不一定更低
- 維護複雜度高很多

### Q: Vercel 免費方案真的夠用嗎？

A: 對於這個專案，完全夠用：
- Dashboard 流量不大
- API 呼叫次數有限
- 主要的重度運算在 Crawler Worker
- 只有商業化後流量很大才需要升級

### Q: 能先用免費方案測試嗎？

A: 可以！建議順序：
1. **Supabase Pro**（您已有，$25/月必須）
2. **Vercel Hobby**（免費，先測試）
3. **Render 免費方案**（測試 Crawler，會 sleep）
4. 測試沒問題後，再升級 Railway/Render 到付費方案

---

## 下一步

**請先提供新的 Supabase Pro 專案資訊，我會立即幫您更新所有配置！** 🚀
